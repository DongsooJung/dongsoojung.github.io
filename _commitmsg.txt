fix(election): 선거구 지도 4종 라이트 테마 가독성 복구

assets/site-theme.css 의 :root[data-theme="light"] 가 공용 토큰(--ink,
--panel, --line, --muted)을 라이트 값으로 덮어쓰지만, 선거구 페이지 인라인
CSS 는 :root(특이도 0,1,0)로만 다크 값을 정의하고 배경은 그라디언트로
하드코딩해 두어 "텍스트만 어두워지고 배경은 다크로 남는" 상태였습니다.
화이트 모드에서 읍면동 목록과 제목이 사실상 읽히지 않았습니다.

- 인라인 <style> 에 :root[data-theme="light"] 오버라이드 블록 추가
- body 배경 그라디언트, 사이드/맵 패널, 필터·검색·목록 아이템, 툴바 pill,
  info-card, legend, Leaflet 줌·attribution·tooltip 을 라이트 팔레트로 전환
- --accent 를 #3ec6b8 -> #0d8577 로 조정해 라이트 배경 대비 확보

다크 모드는 기존 :root 값을 그대로 사용하므로 변화 없음.
